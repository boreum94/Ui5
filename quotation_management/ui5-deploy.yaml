# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: node.t2.quotationmanagement
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://61.97.134.36:8000
          client: '100'
        app:
          name: ZUI5_D22_002
          description: '[SD] 견적서 생성 및 송출'
          package: ZPD2SD_01
          transport: EFXK902465
        exclude:
          - /test/
